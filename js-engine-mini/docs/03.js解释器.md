01. 解释器，就是遍历AST语法树，然后根据Node节点类型，去执行或计算每个节点。
02. 实现一个JS解释器，需要对AST语法树Node节点每个类型做区分判断，主要有以下几种：
* 变量
* 作用域以及作用域链
* 上下文This指向
* 条件判断
* For循环，其中的 break和continue
* 函数部分 Function
* 生成器 Generator
* 异步 Async
03. 三个类保存相关的值
* Scope，作用域类，保存作用域内的值以及作用域链（当前作用域可以找到父级作用域链）
* Visitor，AST树Node节点处理类，里面有函数visitNode(node, scope)，用来处理对应node类型，其中VISITOR是所有类型函数的Map对象，用来快速查询
* Context，上下文类，保存上下文this指向，以及当前作用域
04. 在JavaScript中，对变量的声明通常是绑定在作用域中的，而作用域分为以下几种
* 全局作用域（Global Scope）全局作用域中仅存在一处，即为最上级的环境。
* 函数作用域（Function Scope）函数存在并执行时，内部存储函数作用域
* 块级作用域（Block Scope）每隔block块{}都可产生作用域，如if for while等


05. 上下文的this对象其实指的就是当前作用域，然而我们了解过JS中的this是可以改变的
* call() bind() apply()等函数，当执行到相关函数的时候，需要将传递进来scope的替换成当前的scope
* 箭头函数中的this，this指向外部函数的this对象

### ref
* https://www.npmjs.com/package/acorn
* https://github.com/webfansplz/giao-js/ Mini Javascript Interpreter
* https://juejin.cn/post/6943550570515038245 编译原理实战一：如何用JS实现一个词法分析器？
* https://cloud.tencent.com/developer/article/1886888 分享一篇可视化的JS引擎执行流程
* https://zhuanlan.zhihu.com/p/70458440 JavaScript 究竟是怎样执行的？
* https://qborfy.com/today/20230117.html 理解React中Fiber架构(一)
* https://github.com/qiubohong/qiubohong.github.io/blob/master/code/jsengine/main.js 