{
  "name": "event-bus-ts",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build index.ts --outdir=dist --minify",
    "test": "bun test"
  },
  "dependencies": {
    "@types/node": "^20.4.0",
    "bun-types": "^0.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
